{
  "name": "googlesignindemo",
  "version": "1.0.0",
  "description": "Demo de la API de Google OAuth",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "lite-server -c ls-config.json"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "lite-server": "^2.5.4"
  }
}
